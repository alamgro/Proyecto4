using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RespawLoot : MonoBehaviour
{

    /* Alexander Iniguez November, 2021
     * Put loot with ratio of one collider
     * Collider trigger
     */
    [SerializeField] private Collider Collider;
    [SerializeField] private GameObject[] lootsTypes;
    [SerializeField] private float timer;



    /*un collider
     * el tipo de collider 
     * maximos y minimo spara el rand
     * timer
     * 
     */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private float MaxAndMinCollider()
    {
        float position;
        float min , max;

        min 


        return position;
    }
}
